general:
  branches:
    ignore:
      - gh-pages

deployment:
  deployment:
     branch: master
     commands:
       - git config --global user.name "Rudolph-Miller"
       - git config --global user.email "chopsticks.tk.ppfm@gmail.com"
       - sh deploy.sh
