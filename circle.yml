general:
  branches:
    ignore:
      - gh-pages

dependencies:
  pre:
    - go get -v github.com/spf13/hugo
    - git clone --recursive https://github.com/Rudolph-Miller/vienna themes/vienna

test:
  override:
    - hugo

deployment:
  deployment:
     branch: master
     commands:
       - git config --global user.name "Rudolph-Miller"
       - git config --global user.email "chopsticks.tk.ppfm@gmail.com"
       - sh deploy.sh
